services:
  gateway:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - ENGINE_URL=${ENGINE_URL:-http://host.docker.internal:8000}
      - PORT=8080
      - GIN_MODE=${GIN_MODE:-release}
    restart: unless-stopped